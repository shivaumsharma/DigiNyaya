"""Citizen authentication: phone+OTP and email+password, with account
linking, JWT access tokens and rotating refresh tokens.

This package's `user.id` is the case-ownership identity used throughout
app/main.py. The old Aadhaar-demo login (app/security/auth.py) has been
retired.
"""
